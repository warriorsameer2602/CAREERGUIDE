<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'CareerGuide - Your Career Compass' %></title>
    
    <!-- SEO Meta Tags -->
    <meta name="description" content="<%= description || 'Discover the perfect academic path with AI-powered recommendations, explore government colleges, and unlock your future potential.' %>">
    <meta name="keywords" content="career guidance, government colleges, scholarships, education, student guidance, career counseling">
    <meta name="author" content="CareerGuide Team">
    
    <!-- Open Graph Meta Tags -->
    <meta property="og:title" content="<%= title || 'CareerGuide - Your Career Compass' %>">
    <meta property="og:description" content="<%= description || 'AI-powered career guidance platform for students' %>">
    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= process.env.BASE_URL || 'http://localhost:3000' %>">
    
    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="/images/favicon.ico">
    
    <!-- External CSS Libraries -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Common CSS -->
    <link rel="stylesheet" href="/css/style.css">
    
    <!-- Page-specific CSS -->
    <% if (typeof cssFile !== 'undefined') { %>
        <link rel="stylesheet" href="/css/<%= cssFile %>">
    <% } %>
    
    <!-- Additional head content -->
    <% if (typeof additionalHead !== 'undefined') { %>
        <%- additionalHead %>
    <% } %>
</head>
<body>
