<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/quiz.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <style>
        /* Simple result page styles */
        .results-hero {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 4rem 0;
            color: white;
            text-align: center;
        }
        
        .results-content {
            padding: 3rem 0;
            background: #f8fafc;
        }
        
        .scorecard-container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            padding: 2rem;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }
        
        .main-recommendation {
            text-align: center;
            padding: 2rem;
            margin-bottom: 2rem;
            background: #f0f4ff;
            border-radius: 15px;
        }
        
        .recommendation-badge {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            padding: 1rem 2rem;
            border-radius: 25px;
            font-size: 1.3rem;
            font-weight: 700;
            margin: 1rem 0;
            display: inline-block;
        }
        
        .scores-section, .career-options, .analysis-section {
            background: white;
            padding: 2rem;
            border-radius: 15px;
            margin-bottom: 2rem;
            border: 1px solid #e2e8f0;
        }
        
        .score-bar {
            background: #e2e8f0;
            height: 30px;
            border-radius: 15px;
            margin: 0.5rem 0;
            overflow: hidden;
        }
        
        .score-fill {
            height: 100%;
            border-radius: 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: bold;
        }
        
        .score-fill.science { background: linear-gradient(135deg, #48bb78, #38a169); }
        .score-fill.commerce { background: linear-gradient(135deg, #ed8936, #dd6b20); }
        .score-fill.humanities { background: linear-gradient(135deg, #667eea, #764ba2); }
        
        .careers-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
            margin-top: 1rem;
        }
        
        .career-item {
            background: #f0f4ff;
            padding: 1rem;
            border-radius: 10px;
            text-align: center;
            border-left: 4px solid #667eea;
        }
        
        .btn {
            padding: 0.875rem 2rem;
            border: none;
            border-radius: 25px;
            font-weight: 600;
            cursor: pointer;
            margin: 0.5rem;
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            text-decoration: none;
            font-family: inherit;
        }
        
        .btn-primary {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
        }
        
        .btn-secondary {
            background: #6c757d;
            color: white;
        }
        
        .actions-section {
            background: white;
            padding: 2rem 0;
            text-align: center;
        }
    </style>
</head>
<body>

<!-- Results Hero -->
<section class="results-hero">
    <div class="container">
        <h1>Your Career Assessment Results</h1>
        <p>Based on your responses to <%= results.totalQuestions %> questions</p>
        <div class="completion-badge">
            <i class="fas fa-trophy"></i>
            Assessment Complete! Time: <%= Math.floor(results.timeSpent / 60) %> minutes
        </div>
    </div>
</section>

<!-- Results Content -->
<section class="results-content">
    <div class="container">
        <div class="scorecard-container" id="pdf-content">
            
            <!-- Main Recommendation -->
            <div class="main-recommendation">
                <h2>Your Recommended Career Path</h2>
                <div class="recommendation-badge">
                    <%= results.primaryRecommendation %>
                </div>
                <p><strong>Confidence Level: <%= results.confidence %></strong></p>
                <p>Based on <%= results.answeredQuestions %>/<%= results.totalQuestions %> responses</p>
            </div>
            
            <!-- Stream Scores -->
            <div class="scores-section">
                <h3><i class="fas fa-chart-bar"></i> Stream Compatibility Scores</h3>
                <div class="score-item">
                    <label><strong>Science Stream</strong></label>
                    <div class="score-bar">
                        <div class="score-fill science" style="width: <%= results.scores.science || results.compositeScores.science %>%">
                            <%= results.scores.science || results.compositeScores.science %>%
                        </div>
                    </div>
                </div>
                <div class="score-item">
                    <label><strong>Commerce Stream</strong></label>
                    <div class="score-bar">
                        <div class="score-fill commerce" style="width: <%= results.scores.commerce || results.compositeScores.commerce %>%">
                            <%= results.scores.commerce || results.compositeScores.commerce %>%
                        </div>
                    </div>
                </div>
                <div class="score-item">
                    <label><strong>Humanities Stream</strong></label>
                    <div class="score-bar">
                        <div class="score-fill humanities" style="width: <%= results.scores.humanities || results.compositeScores.humanities %>%">
                            <%= results.scores.humanities || results.compositeScores.humanities %>%
                        </div>
                    </div>
                </div>
            </div>
            
            <!-- Career Options -->
            <div class="career-options">
                <h3><i class="fas fa-briefcase"></i> Recommended Career Options</h3>
                <div class="careers-grid">
                    <% results.careerOptions.forEach((career, index) => { %>
                        <div class="career-item">
                            <div style="font-weight: bold; color: #667eea; margin-bottom: 0.5rem;">#<%= index + 1 %></div>
                            <div style="font-weight: 600;"><%= career %></div>
                        </div>
                    <% }) %>
                </div>
            </div>
            
            <!-- Analysis -->
            <div class="analysis-section">
                <h3><i class="fas fa-lightbulb"></i> Detailed Analysis</h3>
                
                <div style="margin-bottom: 2rem;">
                    <h4 style="color: #48bb78;"><i class="fas fa-star"></i> Your Strengths</h4>
                    <ul id="strengths-list">
                        <% if (results.detailAnalysis && results.detailAnalysis.strengths && results.detailAnalysis.strengths.length > 0) { %>
                            <% results.detailAnalysis.strengths.forEach(strength => { %>
                                <li><%= strength %></li>
                            <% }) %>
                        <% } %>
                    </ul>
                </div>
                
                <div style="margin-bottom: 2rem;">
                    <h4 style="color: #667eea;"><i class="fas fa-compass"></i> Recommendations</h4>
                    <ul>
                        <% if (results.detailAnalysis && results.detailAnalysis.recommendations) { %>
                            <% results.detailAnalysis.recommendations.forEach(rec => { %>
                                <li><%= rec %></li>
                            <% }) %>
                        <% } %>
                    </ul>
                </div>
                
                <div>
                    <h4 style="color: #ed8936;"><i class="fas fa-road"></i> Next Steps</h4>
                    <ol>
                        <% if (results.detailAnalysis && results.detailAnalysis.nextSteps) { %>
                            <% results.detailAnalysis.nextSteps.forEach(step => { %>
                                <li><%= step %></li>
                            <% }) %>
                        <% } %>
                    </ol>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Action Buttons -->
<section class="actions-section">
    <div class="container">
        <button class="btn btn-primary" id="download-pdf">
            <i class="fas fa-download"></i> Download PDF Report
        </button>
        <button class="btn btn-secondary" onclick="printReport()">
            <i class="fas fa-print"></i> Print Results
        </button>
        <a href="/quiz" class="btn" style="background: #f7fafc; color: #667eea; border: 2px solid #667eea;">
            <i class="fas fa-redo"></i> Retake Assessment
        </a>
    </div>
</section>

<!-- WORKING PDF Script -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js"></script>

<script>
// WORKING PDF Download - Based on Your Friend's Code
function downloadReport() {
    console.log('Starting PDF download...');
    
    // 1. Select the HTML element containing the entire report card
    const element = document.querySelector('.scorecard-container');
    
    if (!element) {
        alert('Report content not found. Please try again.');
        return;
    }
    
    // 2. Define options for the PDF - exactly like your friend's code
    const opt = {
        margin: 1,
        filename: 'career-assessment-report.pdf',
        image: { type: 'jpeg', quality: 0.98 },
        html2canvas: { scale: 2 },
        jsPDF: { unit: 'in', format: 'letter', orientation: 'portrait' }
    };
    
    // 3. Show loading
    const btn = document.getElementById('download-pdf');
    if (btn) {
        btn.disabled = true;
        btn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Generating PDF...';
    }
    
    // 4. Generate PDF with delay to ensure content is loaded
    setTimeout(() => {
        html2pdf().set(opt).from(element).save()
            .then(() => {
                console.log('PDF generated successfully!');
                if (btn) {
                    btn.disabled = false;
                    btn.innerHTML = '<i class="fas fa-download"></i> Download PDF Report';
                }
            })
            .catch((error) => {
                console.error('PDF generation failed:', error);
                alert('PDF download feature requires html2pdf library. Report can be printed using the Print button.');
                if (btn) {
                    btn.disabled = false;
                    btn.innerHTML = '<i class="fas fa-download"></i> Download PDF Report';
                }
            });
    }, 1500); // Wait 1.5 seconds for content to load
}

// Print function
function printReport() {
    window.print();
}

// FIXED: Add strengths if empty
document.addEventListener('DOMContentLoaded', function() {
    const strengthsList = document.getElementById('strengths-list');
    
    // Add strengths if list is empty
    if (strengthsList && strengthsList.children.length === 0) {
        const results = <%- JSON.stringify(results) %>;
        const strengths = [];
        
        // Generate strengths based on scores
        if (results.aptitudeScores) {
            if (results.aptitudeScores.numerical >= 80) strengths.push('Exceptional mathematical and quantitative abilities');
            else if (results.aptitudeScores.numerical >= 65) strengths.push('Strong numerical reasoning skills');
            else if (results.aptitudeScores.numerical >= 50) strengths.push('Good basic mathematical skills');
            
            if (results.aptitudeScores.verbal >= 80) strengths.push('Outstanding verbal and language comprehension');
            else if (results.aptitudeScores.verbal >= 65) strengths.push('Strong communication and language skills');
            else if (results.aptitudeScores.verbal >= 50) strengths.push('Good verbal reasoning abilities');
            
            if (results.aptitudeScores.logical >= 80) strengths.push('Superior logical reasoning and problem-solving abilities');
            else if (results.aptitudeScores.logical >= 65) strengths.push('Strong analytical and logical thinking skills');
            else if (results.aptitudeScores.logical >= 50) strengths.push('Good logical reasoning capabilities');
        }
        
        // Ensure at least some strengths
        if (strengths.length === 0) {
            strengths.push('Well-rounded academic abilities across multiple subjects');
            strengths.push('Balanced approach to problem-solving');
            strengths.push('Good potential for diverse career paths');
        }
        
        // Add to list
        strengths.forEach(strength => {
            const li = document.createElement('li');
            li.textContent = strength;
            strengthsList.appendChild(li);
        });
    }
    
    // Attach PDF download function
    const downloadBtn = document.getElementById('download-pdf');
    if (downloadBtn) {
        downloadBtn.addEventListener('click', downloadReport);
    }
    
    console.log('Results page loaded successfully');
});

// Make functions global
window.downloadReport = downloadReport;
window.printReport = printReport;
</script>

</body>
</html>
